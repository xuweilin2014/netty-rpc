package com.newlandframework.rpc.registry.zookeeper;

import com.newlandframework.rpc.registry.AbstractRegistryFactory;
import com.newlandframework.rpc.registry.Registry;
import com.newlandframework.rpc.util.URL;

public class ZookeeperRegistryFactory extends AbstractRegistryFactory {
    @Override
    public Registry createRegistry(URL url) {
        return null;
    }
}
